# Production gap analysis

Status is based on executed/reviewed repository evidence as of 2026-09-12.
Absent evidence is not treated as a passing requirement.

| Severity | Gap | Why it blocks or constrains release | Required closure evidence |
| --- | --- | --- | --- |
| CRITICAL | One replication correctness defect is known open: followers serve, and a failover can commit, a leader transaction's uncommitted or rolled-back row versions (HIGH row below). Six storage defects were found and fixed after this audit opened (the WAL fsync latch, the poisoned-create path, the missing-segment fail-open, page images outrunning their undo records, undo records not `fsync`ed before the WAL, and page images appended out of the order they were copied — rows below); their absence beforehand reflected the reach of the tests then available, not the engine's state | An absence of discovered defects is not a pass, and every one of them sat on an I/O-failure, recovery or replication path | Close the follower row. Retain continuous crash/corruption gate evidence. Fault injection now reaches past the WAL barrier — page write/read, data fsync, directory fsync, nonce reservation, undo barrier, backup and restore — via `tests/fault`, and `tests/upgrade` replays shipped-binary fixtures; keep extending both as new durable paths land |
| HIGH | P28 installer exit gate remains open; macOS execution is host-blocked. Native Windows was de-scoped by the user (log #290): no Windows packages are built and the server and tool binaries refuse a `GOOS=windows` build, so Windows hosts run the Linux packages under WSL 2 — a path not yet execution-tested on a Windows host | The repository's current release gate is unmet | Complete or explicitly de-scope with release authority. M2 is now closed: `internal/crypto` supports a second independent unlock key sealing the same KEK (keystore `NSKS` v2, written only on opt-in), exposed as `nextsql key status / add-recovery / verify-recovery / remove-recovery / recover` for both keystores, and `nextsql setup --recovery-key-out` plus the Setup wizard export and verify one for each keystore during a first install, holding Finish until the operator confirms they saved both. Verified live end to end: a wizard-shaped install, both root key files deleted, both keystores recovered, pre-loss rows intact and new writes accepted. What remains is macOS execution, which still needs that host |
| CLOSED (was HIGH, log #289) | Each single-row commit wrote ~18 KB, because redo logged a full 16 KiB page image per touched page per commit | A 64-connection insert load saturated the drive within seconds | Closed by page-delta redo (option chosen by the user: deltas after a full image, not compression, which would leak page compressibility through ciphertext length). WAL control file v2 and replication batch v2 are the version gates; every recovery, repair and replica path fails closed on a delta whose base does not match. Measured over the wire on a default `nextsqld`: 2.3–2.6 KB written per insert, against 18.2–19 KB with `wal_page_deltas=off`, in alternating runs. Existing v1 databases keep full images under the default `auto` and stay readable by v0.0.1 (`tests/upgrade`). Zero-filled WAL segments (0.89 vs 1.44 ms p50 fsync) remain an independent, unattempted gain |
| MEDIUM (open, log #289) | Sustained insert throughput on a saturated device is not characterized on a dedicated host | The only host available is a desktop with 5–7% I/O pressure and no NextSQL running. Throughput stalls there to near zero for seconds in both redo modes, including runs that had written only a few MiB, so a sustained-throughput claim from it would measure the host | Repeat the alternating 20 s, 64-connection insert run (`off`/`auto`, settle gap between runs) on an idle, dedicated ext4/xfs host. Record ops/s over time, fsync latency and bytes per insert, and attribute any remaining collapse to WAL, data-file flush or device before tuning |
| MEDIUM (open, log #287) | Replicated commits still serialize across the Raft round trip | Single-node commits now share fsyncs (group commit, log #287), but `commitAndReplicate` holds `replMu` for a whole commit including `Replicate`, so cluster write throughput is one commit per quorum round | Batch proposals: gather concurrent held commit records into one Raft entry and resolve each against the batch's outcome, preserving the in-doubt handling of log #79. Needs partition, leader-kill and transfer tests under concurrent writers |
| MEDIUM (open, log #287) | One matrix run of `TestRollingUpgradeDrainWithLeaderTransferNoTransactionLoss` reported `client-acknowledged=1 committed=0` | An acknowledged-write report cannot be closed on a pass rate | It passed 8/8 alone, 30/30 under concurrent load, and 24/25 under heavy `-race` load; the one failure there was a convergence deadline, not a loss. The replicated commit sequence is unchanged by log #287. Close with a root cause, for example whether the ground-truth count read can be served by a node that has not caught up, or a reproduction |
| MEDIUM (was HIGH) | No full production release command previously combined storage, WAL/recovery, protocol, auth and backup evidence | Individual tests can hide an unrun required category | CI is now wired: `.github/workflows/production-gate.yml` runs `make test-production` on `master`/version tags and retains its complete log for 30 days. **The profile itself was incomplete until log #256**: it ran 24 packages and none of `internal/executor`, `internal/sql/...`, `internal/replication`, `tests/crash`, `tests/ha` or `tests/integration`, so a wrong query answer, a failed failover or a broken driver could not fail it — the six aggregate defects of logs #250-#254 would all have passed. It now runs 37 packages including a `-race` pass over the executor — 36 after log #256, plus `internal/limits` in log #257, which is the catalog every other package validates against and the source `docs/limits.md` is generated from, and which no profile had run. A green local run with logs #271-#280 in the tree is retained at `docs/production/evidence/2026-09-12-test-production.md` (exit 0, 49 packages, `filesystem: ext2/ext3`), which is the evidence for the statement-nesting bound, the dirty-write refusal and the subquery-correlation fix, and which includes `tests/upgrade` replaying shipped-`v0.0.1` fixtures against catalog `NSCT` v14. An earlier **local** green run is retained at `docs/production/evidence/2026-09-10-test-production.md` (exit 0, 37 packages, no failure, panic, data race or timeout, `filesystem: ext2/ext3`). **The first green hosted run is now retained** at `docs/production/evidence/2026-09-10-test-production-hosted.md` (Actions run 34407898651, `v0.0.1` at `102dc5f`, exit 0, 48 packages, no failure, panic, data race or timeout, `filesystem: ext2/ext3`, 10m50s) — the log is copied into the repository because the workflow artifact expires 2026-10-09. See `AFTER.md`. The first hosted run to include the new packages went **red**, and the gate did its job: `internal/replication`'s `TestStrongReadBarrierRejectsIsolatedLeader` failed, and the cause was a test-harness precondition bug that also reproduces on an idle machine about one run in ten (log #258) — `startRaft` returned as soon as the *leader* held the full voter set, so a follower could still be on the older configuration when the test partitioned the cluster, deadlocking the survivors permanently. Fixed in the helper, with a local green `make test-production` afterwards retained at `evidence/2026-09-10-test-production-log258.md`; the required artifact is still a green **hosted** run. |
| MEDIUM (was HIGH) | The durable test profile must not run on a RAM-backed filesystem | `/tmp` is `tmpfs` on at least one development host, so an unconfigured run fsyncs into RAM and proves nothing about durability | CI sets `NEXTSQL_REQUIRE_DURABLE_FS=1` and retains the `test scratch: … (filesystem: …)` line for 30 days. The retained local run above was made with that variable set and records `filesystem: ext2/ext3` — so the refusal path is exercised and the volume is a real block device on this host. The first hosted run is now retained (see the row above) and records `filesystem: ext2/ext3` on the runner — a real block device, not `tmpfs` — so the refusal path is exercised in CI as well as locally. |
| CLOSED (was HIGH) | Limit model was fragmented; frame, SQL and descriptor limits were coupled and many operational limits were constants | Legitimate load and adversarial input could not be independently controlled | Closed: `internal/limits` is the single sorted catalog of operational defaults, absolute ceilings, units, zero semantics and rationale. It separates wire/frame/SQL/parameters/prepared state/results; memory; concurrency; time; and storage controls. `config.Load`, programmatic `Config.Validate`, `Marshal`/`WithSetting`, and `nextsqld` all use it; catalog, range, parser/validator, round-trip, protocol/server and race regressions cover it. `docs/limits.md` is generated and checked. Structural/format limits remain deliberately local to their encoding and are inventoried there. |
| CLOSED (was HIGH) | Disk-full, short-write, failed-fsync and EIO coverage is not demonstrated as a release profile | Durability claims require explicit I/O failure behavior | Closed: `tests/fault` is a named profile (`make test-fault`; also run by `test-pr`, `test-production`, `test-chaos`, `test-nightly`). The seam covers reads, positional and sequential writes, `fsync`/`fdatasync` and directory fsync, identifies the file and offset so one device can fail while another works, and reproduces genuine torn writes (`diskio.ShortWrite`). Eleven engine-level cases: checkpoint under a failing page write and under a failing data fsync, an unreadable page, a torn WAL write, `ENOSPC` that clears, a failed create, a failed nonce reservation (no AES-GCM nonce reuse), an unreadable log during redo, a failed undo barrier, and backup/restore on a full device. Each asserts its fault actually fired |
| CLOSED (was HIGH) | A failed WAL `fsync` did not latch, so the next flush declared the un-synced region durable and acknowledged a commit that a crash would lose | Acknowledged-commit loss after a transient device error — a durability violation | Closed: `internal/wal` latches a failed durability barrier; regression tests at the WAL boundary (`TestFailedSyncLatchesAndRefusesLaterCommits`) and end to end (`storage.TestCommitFailsClosedAfterDurabilityFault`), both verified to fail without the fix |
| CLOSED (was undetected) | `wal.Open` accepted a WAL whose segments no longer covered the redo boundary, so a database came up silently missing every acknowledged commit after that boundary and reported `status ok` | The next checkpoint installed over the gap, making the loss permanent; found by deleting the WAL segment from a retained fixture as a negative control | Closed: `wal.Open` fails closed with a `corruption` error naming `durable_lsn` and `redo_lsn` when no segment exists while anything was acknowledged, or when the oldest segment starts past the redo boundary. `wal.TestOpenRefusesMissingSegments` covers both, and a never-acknowledged empty log still opens; both failing cases were verified against the un-fixed code |
| CLOSED (was undetected) | A create that failed after the data file existed left the file behind, so every later create at that path returned `already_exists` | A full or failing device during `nextsql init` (or a hosted database create) permanently poisoned the path: the operator had to know to delete a file no usable database was ever built on | Closed: `file.Create` removes the file when its directory entry cannot be made durable, and `storage.open` unwinds exactly what a failed create made — the data file, the integrity sidecar, and only those WAL/undo directories that did not already exist. `tests/fault` covers the directory-fsync and undo-barrier variants; both were verified to fail without the fix |
| CLOSED (was undetected) | Aggregates over `system.*` tables failed with `unknown function`, and the two official-driver live suites that assert one were the only ones not run by any gate | A documented catalog query was broken, and the suites that would have caught it sat failing with no signal — a coverage hole, not only a SQL one | Closed: `internal/executor/system.go` answers scalar aggregates by driving `aggregate.Hash` with no grouping columns, so the accumulators are the user-table ones (`COUNT(*)` over an empty catalog is one row holding `0`; `LIMIT`/`OFFSET` bound the result, not the input; a bare column beside an aggregate is rejected as the binder rejects it). Eight regressions in `internal/executor/system_aggregate_test.go`, all verified to fail against the un-fixed evaluator. `tests/integration/drivers_test.go` now runs the Python and Ruby unit **and** live suites alongside Node/Bun/PHP, and each live gate fails if the suite skipped itself rather than connecting |
| CLOSED (was undetected) | `GROUP BY` on a computed or positional expression returned a wrong answer, and `HAVING COUNT(*)` could never match its selected aggregate (computed grouping is now implemented — log #253; positional grouping stays refused) | A silently wrong aggregate result is the worst failure class in the priority order — the query reported one collapsed group as though it were the grouped answer | Closed: the planner built group ordinals by dropping any item that was not a resolvable column, which collapsed the whole input into one implicit group rather than disabling grouping (`SELECT a + b, COUNT(*) ... GROUP BY a + b` answered a single bogus row). `GROUP BY` now requires a column reference, refused at bind time with the planner failing closed behind it; `binder.grouped` no longer treats two unidentifiable expressions as the same one. Separately, `HAVING` matched select items with `reflect.DeepEqual` against expressions already rebuilt with an empty **non-nil** `Args` slice, so a star aggregate never matched — both sides are now normalised identically. Six regressions in `internal/executor/group_by_correctness_test.go`, each verified to fail against the un-fixed code |
| CLOSED (was undetected) | Aggregates over a computed argument read as `COUNT(*)`, and an aggregate inside a larger expression answered an uninitialised empty cell (both are now implemented, not merely refused — logs #252 and #253) | Two more silently wrong aggregate results, found by using the log #250 defect as a search heuristic rather than by any test | Closed: an aggregate argument resolves to an input column ordinal whose "unresolved" value `-1` already meant *star*, so `COUNT(a + b)` counted every row and `MIN`/`MAX` over an expression returned `NULL`; and with no projection running after aggregation, `SELECT COUNT(*) + 1` fell through to a zero `types.Value`. Both are now refused at bind time. Window functions project after evaluation and are deliberately unaffected. Regressions in `internal/executor/group_by_correctness_test.go`, each verified against the un-fixed code. All 24 decoder fuzz targets, NULL/three-valued `WHERE`, `NOT IN` over a NULL-bearing subquery, outer-join cardinalities and `SUM` past `INT64` max were also checked and are correct |
| CLOSED (was HIGH) | Upgrade compatibility is not demonstrated against retained prior-release fixtures | A format change could strand production data | Closed: `tests/upgrade` (`make test-upgrade`; also run by `test-pr`, `test-production`, `test-nightly`) replays retained fixtures — real data directories written by the shipped `v0.0.1` binaries, clean and `SIGKILL`ed, with the query results and compatibility catalog that release printed. The current build must match every recorded result, keep the fixture principal's grants unchanged, write, restart, and backup/restore, and must leave every on-disk family inside the generating release's readable window (rollback preservation); with `NEXTSQL_UPGRADE_OLD_BINDIR` the released binaries themselves re-read the upgraded directory. Cut one pair per release with `scripts/make-upgrade-fixture.sh`. What remains is procedural: `RELEASING.md` now requires it |
| CLOSED (was MEDIUM) | Public error codes did not emit the stable `ERR_*` vocabulary on the wire | Client retry/diagnostics received only legacy lowercase internal classes | Closed by capability negotiation rather than a frame-version bump, which would have disconnected every existing client (`ReadFrame` accepts only version 1). `Hello.flags` bit `0x0002` requests the taxonomy; the server masks the request to what it implements, echoes it in an optional trailing `HelloOK` flags field, and appends the `ERR_*` name as an optional trailing error field. The legacy lowercase `code` is never replaced, so existing retry checks are untouched, and a client that requests nothing gets byte-identical v1 frames — asserted by a hand-rolled v1 client in `tests/integration`. All six official drivers request it and expose the name (`docs/error-codes.md` has the per-driver field table), verified live against a real server. `FuzzDecodeError`/`FuzzDecodeHelloOK` enforce canonical encoding; both found and fixed a real malleability where an empty trailing field decoded as an absent one. |
| CLOSED (was MEDIUM) | Documentation had a geometry-limit contradiction | Operators could make unsafe sizing decisions | Closed: fixed native `LINESTRING`/`POLYGON` stays at 256 vertices (`types.MaxGeoVertices`); general OGC `GEOMETRY`/`GEOGRAPHY` separately permits 65,536 total vertices, nesting depth 8 and 4,096 parts (`types.MaxSpatialVertices` / `MaxSpatialDepth` / `MaxSpatialParts`). The public limits, protocol/security reference tables and production report now distinguish the two. |
| CLOSED (was undetected) | Any authenticated user could crash the whole server with a single statement: a 2 MB `SELECT 1+1+…` or deeply parenthesised expression exhausted Go's 1 GB goroutine-stack ceiling and killed `nextsqld` with `fatal error: stack overflow` | A remote denial of service available to every principal, well inside `max_statement_bytes`, taking every other session down with the process — and unrecoverable, since a Go stack overflow cannot be recovered | Closed (log #271): statements nest at most `ast.MaxNestingDepth` (4096), charged during parsing — including per operator in the iteratively-parsed chains that produced deep trees from a shallow parse — with an iterative post-parse check as the authority on the finished tree, and the catalog's stored-expression decoder bounded separately. Eleven shapes covered in `internal/sql/parser/nesting_test.go`, all verified to fail against the un-fixed parser; `FuzzParse` asserts the invariant. Measured: a hostile 2 MB statement refused in 4-24 ms with flat memory (was 646 MiB per request) |
| CLOSED (was undetected) | Two transactions could both write the same row, both commit, and one write would vanish with nothing reported — at `SNAPSHOT` (the default) and `READ COMMITTED` | A lost update is the worst class of defect in the priority order, and it was reachable with two ordinary connections doing ordinary `UPDATE`s | Closed (log #276): a write whose target version belongs to a different, still-running transaction is refused as `serialization` at every isolation level, since SQL forbids a dirty write at all of them. The engine's key locks could not cover it — `lockWrite` skips locking below `SERIALIZABLE` when only one transaction is live at that moment, so the first writer of a row routinely left no lock for a later writer to block on. Regressions in `internal/executor/dirty_write_test.go`, each verified against the un-fixed engine |
| CLOSED (was undetected) | A CTE inside a subquery silently answered the wrong question: `WHERE x IN (WITH c AS (...) SELECT ... FROM c)` matched every row and its `NOT IN` matched none | Another silent wrong answer, reachable from hand-written SQL and not only from the view expansion that exposed it | Closed (log #278): correlation resolved the inner relation with a table lookup, so a CTE name yielded no schema and every unqualified column was replaced by the outer row's value. Only explicitly qualified outer references are correlated when the inner columns are not visible; ordinary correlated subqueries are unchanged and covered alongside in `internal/executor/cte_subquery_test.go` |
| CLOSED (was undetected) | `NOT` bound more tightly than the comparison it negated, so `NOT a = b` meant `(NOT a) = b` | Usually a confusing "NOT requires boolean" refusal, but the opposite answer over a boolean-valued expression | Closed (log #271): `NOT` parses at predicate precedence, below comparison, `IS NULL`, `BETWEEN`, `IN` and `LIKE`. A `NOT` inside an operand still parses, so nothing that worked stops working |
| CLOSED (was undetected) | A checkpoint taken while a running transaction held a dirty page could lose an acknowledged commit after a crash | The redo boundary was the log's end, but a page a running transaction dirtied cannot be flushed, so a committed image of that page sat below the boundary while the page on disk was older. Recovery started past it and the acknowledged row was gone | Closed (log #289): the boundary is the minimum of the log end at flush start, the oldest unstamped logged change, the oldest dirty frame's first logged LSN, and the oldest running transaction's `Begin`. `tests/crash/checkpoint_redo_test.go` pins the dirty-frame and running-transaction terms at the storage layer, in both full-image and page-delta modes; each fails with its term removed |
| CLOSED (was undetected) | A rolled-back transaction could come back committed after a crash: half of a transfer, with no undo | Recovery only undid transactions with neither a commit nor an abort record, and the transaction manager defaults an unknown id to committed. A rollback whose logical undo had not yet reached the data file was replayed from a later commit's page image as committed data | Closed (log #289): `recovery.NotCommittedUntil` returns open and aborted transactions; the engine applies undo to both and registers both as aborted. `TestRollbacksWithPageDeltasSurvivePowerLoss` (16 sessions of transfers with deliberate and conflict rollbacks, a checkpoint mid-stream, power loss, exact total and index checks) lost money in both redo modes before the fix and loses it again with the fix reverted |
| CLOSED (was undetected) | A logged page image could reach the WAL ahead of the undo records for the uncommitted versions it carried | Undo records are buffered in memory. A commit wrote the buffer before copying its dirty pages, leaving a window in which another transaction could log undo and change a shared page, and a split's system transaction logged images with no write at all. A crash then lost the buffered record while redo installed its version: a committed row lost or a rolled-back transfer half-applied. `TestRollbacksWithPageDeltasSurvivePowerLoss` failed about one round in 45 (nightly gate, 2026-09-13 and 2026-09-16) | Closed (log #300): `appendPageStatesLocked` writes the undo buffer after the images are copied and before they are appended; btree logs undo before changing a page, so every version in an image has its record written first. `TestPageImageDoesNotOutrunTheUndoItCarries` fails deterministically in both redo modes without it. Process crash only; power loss is the undo `fsync` row (closed in log #301) |
| CLOSED (was undetected) | A committed page image could reach the WAL after a newer image of the same page, and recovery ended on the older one | A commit copied its dirty pages, released `pageMu`, and appended the copies once it had the engine mutex again; another transaction could change a page, copy it and append first. The older copy took the higher LSN and redo, replaying in LSN order, ended on it: hundreds of acknowledged rows lost in one crash, their leaves orphaned from the tree. Predates group commit and page deltas; invisible until something slowed the WAL flush — the undo `fsync` did, and so would a slow disk | Closed (log #301): `flushDirtyImages` and `logStructure` hold `pageMu` for reading from the copy through the append (lock order `pageMu` → `mu`, as documented). `TestConcurrentCommitsSurvivePowerLossWithSlowFlush` (1 ms added to each flush) failed with the hold released and passes with it; 150 rounds of the original test with the delay clean |
| CLOSED (was undetected) | A filtered sequential scan materialized the entire table outside the per-query memory budget | `WHERE k = $1` over a large table held every decoded row before filtering; I/O was charged but memory was not, an out-of-memory path | Closed (log #288): the predicate is evaluated during the scan when that is provably equivalent and concurrency-safe. Tested against a forced collect-then-filter oracle; heap under 64 concurrent filtered scans fell from 1,441 MiB to 400 MiB at peak and 632 MiB to 21 MiB after |
| CLOSED (was undetected) | Acknowledged rows could be lost after power loss with concurrent writers | A B+tree split took effect immediately but was logged only by its own transaction's commit. If that transaction never committed, a committed transaction's durable image could point to, or live in, a page that was never logged: after a crash the page was unreadable and committed rows were gone. It predates group commit | Closed (log #287): a successful page-allocating or page-dropping operation logs its pages as a committed system transaction, and failed appends stay pending and unflushable. `tests/crash` `TestConcurrentCommitsSurvivePowerLoss` (32 concurrent sessions, power cut mid-stream) passes 36/36 rounds; with the fix reverted it fails 2/12, and a negative control that acknowledges before durability loses rows as it must |
| CLOSED (was undetected) | Concurrent maintenance writers committed each other's transactions and leaked one | Tombstone purge used the engine-global `e.txn` slot for begin and commit, so two purges running together committed each other's transaction. One stayed in `writers` forever with pages that could never be flushed, pinning buffer memory without bound | Closed (log #287): explicit exclusive write handles, with page changes inside `Enter`/`LeaveOp`. `TestMaintainIndexConcurrentWrites` passes 15/15 under `-race` (it failed 2/8 before) |
| CLOSED (was undetected) | Deadlocks that formed after a lock changed hands were never detected | Wait-for edges were not rebuilt when a lock was granted, so a cycle closed through the new holder was missed and both transactions waited out the 30 s statement budget. Under concurrent writers this held every admission slot | Closed (log #287): edges are rebuilt after releases and after immediate grants. Two deterministic `internal/txn` regressions, each verified to fail without its half of the fix |
| CLOSED (was undetected) | Per-statement admission starved the transactions that held the contended locks | Statements waiting on a row lock filled the in-flight slots, so the lock holder could not get a slot for its next statement; readers failed with `admission queue wait exceeded` | Closed (log #287): a transaction keeps its admission slot from the statement that opens it until it ends. `TestSustainedMixedLoadKeepsExactInvariant` (32 writers, 16 snapshot readers, connection churn, TLS, exact total invariant) passes 8/8 under `-race` |
| CLOSED (was undetected) | A logical export/import replaced stored NULLs with column defaults, and its import test could not notice | INSERT filled any NULL cell from the default, and import replays rows as INSERTs, so a restore changed data, including fabricated `NOW()`/`UUID()` values. The export gate only probed each table with `LIMIT 1` | Closed (log #287): defaults fill only omitted columns, and the export gate compares every imported row to the dump. With the old rule restored, `Export` refuses to publish |
| CLOSED (was HIGH) | Commits serialized on one fsync each | Single-row inserts stayed at ~700/s from 1 to 64 connections | Closed for single-node deployments (log #287): WAL flush and the commit durability wait run without their mutexes; 16 connections went from 707/s to 3,441-4,983/s in-process on ext4. The replicated path is a separate open row above |
| CLOSED (was LOW) | Adding a keyword could break a stored view body | View bodies are text parsed on every use | Closed (log #287): new views are stored with every identifier quoted, a token-identical rewrite that is fuzzed and checked. Older views that stop parsing are named in a startup warning, and `CREATE OR REPLACE VIEW` repairs one |
| CLOSED (was undetected) | A cancel could permanently disable a TLS session, and the Go driver's late cancel killed the next statement | The server interrupted a cancelled statement by pulling the whole connection deadline to "now". A write cut off that way leaves a TLS record half-sent and latches crypto/tls's write side, so the session kept reading and running requests but could never answer; the client hung for the idle timeout and then saw `tls: bad record MAC`. The Go driver's asynchronous cancel could also arrive after its statement finished and cancel the next one | Closed (log #285): a cancel stops the statement's context and interrupts only the flow-control read, never a write; a cancel with nothing running is a no-op; the Go driver waits for an in-flight cancel before releasing the connection and bounds it. `tests/integration/cancel_stability_test.go` holds the server mid-write through a gated listener. Both regressions were verified to fail against the old code, and the formerly intermittent subscribe test passes 10/10 under `-race` |
| CLOSED (was undetected) | Unauthenticated clients could drive the server out of memory through password hashing | Every Argon2id verification allocates 64 MiB before authentication, and nothing bounded concurrency: 64 wrong-password clients took the heap from 9 MiB to 3.8 GiB | Closed (log #285): `max_concurrent_password_hashes` (default one per four CPUs, at least two) gates hashing process-wide. A burst of 120 logins now peaks at ~0.5 GiB and completes 70% faster than unbounded. Covered by `tests/integration/connection_burst_test.go` under `-race` |
| CLOSED (was undetected) | A parameterized lookup never used an index | `WHERE id = $1`, the form every driver sends, planned as a full scan: a primary-key lookup took 31 ms on a 32K-row table | Closed (log #286): bound values compared directly with a column are substituted before planning, and such plans bypass the plan cache. p50 is now 150 µs (~6,000 lookups/s on one connection). Answer equivalence is tested against an unsubstituted oracle, and the cache-bypass regression returns the wrong row when reverted |
| CLOSED (was undetected) | Explicit `ANALYZE` failed on any table wider than about four columns | The statistics record exceeded the half-page catalog record limit (~2.1 KiB per column), so the planner had no statistics for most real tables | Closed (log #286): `fitStats` reduces detail in a fixed order until the record fits; row, NULL and distinct counts are always kept. Tested at 4–120 columns and verified to fail when reverted |
| CLOSED (was undetected) | A database with a plain `GEOGRAPHY` column could not be logically exported | `GEOGRAPHY` defaults to SRID 4326, so its canonical DDL renders as `GEOGRAPHY(Geometry, 4326)`, and the parser rejected `Geometry` as a subtype. `nextsql export` failed closed at its built-in import test, so no logical export of such a database was possible | Closed (log #284): `types.GeomSubAnyName` is the one spelling of "any subtype", used by both the renderer and `GeomSubByName`. `internal/xport/types_roundtrip_test.go` exports and re-imports such a table and fails with the original export error when the fix is reverted. Found by `tests/docs/types_test.go`, which now requires every declarable column type's rendered DDL to parse back to the same type |
| MEDIUM (partly closed) | Multi-day soak, constrained-memory profiles and benchmark comparison artifacts are not current release evidence | Predictability under sustained load is unproven without dated runs | A dated `constrained-memory-v1` run is now retained at `docs/production/evidence/2026-09-09-btree-soak-constrained.md` (10M operations over 8,192 pool pages under `GOMEMLIMIT=512MiB`/`GOGC=25`, `NEXTSQL_REQUIRE_DURABLE_FS=1`, ext4, PASS/exit 0, 34:01 wall, 132 MiB peak RSS). Producing it exposed two defects in the evidence plumbing itself, both fixed: every retained report recorded `elapsed_wall=mm` because the metrics extractor split GNU `time` output on `:` while the elapsed-time **label** contains colons — the headline number was never measured; and the artifact filename was the hardcoded literal `btree-100m-p16`, which described neither profile, so two different profiles produced names claiming the same shape. Still open: the normal 100M profile and the multi-day run — and the 100M profile is now **sized**, which it was not before. Started on this host it reaches 4M operations in 20 minutes and its own projection grows as the live set does: 3h04m at 1M ops (594K live), 5h58m at 2M (1.18M live), 7h34m at 3M (1.75M live), 7h56m at 4M (2.31M live), as throughput decays 31,369 → 3,360 ops/s. It needs a dedicated run of **8+ hours** on an idle machine, not a slot inside a working session; the attempt here was stopped and its partial artifacts discarded rather than retained as if complete. |
| CLOSED (was MEDIUM) | Raft election timings were hardcoded while the documentation presented them as defaults | `docs/ha.md` said "Default Raft timeouts: heartbeat / election 250 ms, lease 200 ms" and warned against lowering them, but `internal/replication` set them from constants and `Config` exposed no field, so an operator on a slower link or a contended node could not raise them. A cluster that cannot complete an election round inside its election timeout re-campaigns rather than converging — observed directly, twice, under gate load | Closed: `replication.Timings` carries all four intervals, `Config.Timings` accepts them, and `Open` resolves each unset one to its default and validates the set before starting Raft. `raft_heartbeat_ms` / `raft_election_ms` / `raft_leader_lease_ms` / `raft_commit_timeout_ms` are catalogued limits with flags and config keys. **The coupling that reverted the first attempt (log #256) is what the fix is built around**: the healthy-contact window is five of the *configured* heartbeat, not five of the default one, so `ReplicaHealth`, `FollowerReadHealthy` and the default `MAX STALENESS` of a `BOUNDED` read all widen with a raised heartbeat instead of judging every follower unhealthy between contacts — an executor gate names its own default window through `StalenessDefaulter`. The two protocol relationships (lease ≤ heartbeat ≤ election) are enforced at configuration time by `limits.CheckRaftTimings`, which `internal/config` and `internal/replication` share so the rule exists once; a sweep asserts every set `Resolve` accepts is one `raft.ValidateConfig` accepts. Coverage of its own, as the row required: live election, replication, **failover** and **partition** tests run on non-default intervals (`internal/replication/timing_cluster_test.go`), the freshness boundary is checked directly as a pure decision, and each assertion was verified to fail against the un-fixed code. `internal/limits` — which no gate ran — is now in the release profile |
| CLOSED (was MEDIUM) | NextSQL Admin's web-UI suites are not run by any gate, and its accessibility audit had been failing continuously as a result | `internal/admin/frontend`'s four suites (`typecheck`, `test:studio`, `test:setup`, `test:revision`, `test:a11y`) appear in no `Makefile` target and no workflow. `npm run test:a11y` — the real-Chrome axe WCAG 2.2 AA audit over Setup, Operations and Studio — was red, and had been since roughly log #218: because axe throws on the first violation, every check behind it, in all three modes, had gone unrun for dozens of log entries. One assertion added during that window (the audit-chain failure colour) had never executed once and was pinned to a value the flow does not produce | Partly closed (log #261): the audit is green end to end and `make test-admin-ui` runs all four suites, so the profile is discoverable by name. The contrast defects it was stuck on are fixed as a class in `src/shared/shared.css` rather than one token at a time — rui 0.0.9's four `-600` status ramps and the semantic aliases onto them, the light `--color-muted-foreground` on rui's own `--color-muted`, and `CodeBlock`'s syntax-highlight map — and the never-run assertion now derives its expectation from the live theme. Closed in log #280: `production-gate.yml` now runs `make test-admin-ui` as its own `admin-ui` job — Node 22, `npm ci`, an explicit Chrome-discovery step that fails the job rather than skipping the audit silently, and a retained log. It is a separate job from the durability profile because it needs a browser rather than a durable filesystem, and because a UI failure should read as a UI failure. What remains is only the first green **hosted** run of that job, like the profile row above. |
| CLOSED (was LOW) | Test profile names were not standardized | Developer/CI invocation was less discoverable | Closed: named `make test-pr`, `test-production`, `test-fault`, `test-upgrade`, `test-nightly`, `test-chaos`, and `test-soak` targets are published and the production-gate workflow retains each CI evidence log for 30 days; `RELEASING.md` makes review of the release artifact mandatory. |
| CLOSED (was MEDIUM) | Every temporary test database preallocated the production ~256 MiB runway (`DefaultPreallocAheadPages`), because no suite chose one | A test that only checks backup listing and sorting built three databases plus three backups, so the profile wrote far more than it needed and a killed run stranded hundreds of MiB. The runway was also completely untested | Closed per suite, by measurement. `internal/backup` shrinks it in `TestMain` (201s → 3.3s on ext4). `internal/storage/btree` keeps the default: it does bulk inserts, which the runway exists for, and is worse without it (104s vs 115s). New `internal/storage/file` tests cover the runway at the production default, including that `fallocate` reserves real blocks rather than a sparse file |
| CLOSED (was HIGH; earlier closed as "not a defect") | Undo records were written, but not `fsync`ed, before a WAL image that carries the versions they reverse | The earlier closure said undo state is re-derivable by redo. It is not: a committed page image (a commit's, or a split's system transaction) can carry another transaction's uncommitted row version, and only that transaction's undo record holds the value it replaced. On power loss the WAL could be durable while the undo write sat in the page cache; redo then installed the version and undo could not reverse it — a committed row lost or changed. A torn undo tail also lost every record appended after it on the next open | Closed (log #301): the WAL runs `undo.Log.Sync` before writing any record to a segment (`wal.Log.SetBeforeWrite`), fsyncing only when unsynced undo bytes exist; a failed undo write or fsync latches; `undo.Open` cuts a torn tail. `TestPageImageDoesNotOutrunTheUndoItCarriesAcrossPowerLoss` and the randomized `TestRollbacksWithPageDeltasSurvivePowerLoss` (now also discarding unsynced undo bytes) fail every round without the barrier. **Cost, measured** (ext4, alternating builds, p50): about one extra fsync per commit group — single-connection update 2.45 → 3.6 ms, insert 1.26 → 2.4 ms; 16 connections 2.4–2.5 → 4.6 ms, unstalled throughput ~5,400 → ~3,200 tps. Carrying the old version in the WAL's `Undo` record would remove the cost (HIGH follower row) |
| HIGH | Followers serve, and a failover can commit, a leader transaction's uncommitted or rolled-back row versions | A leader's committed page image can carry another transaction's uncommitted version. Followers install it, but have no undo record for it and register only committed transaction ids, and an unknown id defaults to committed: a `STALE`/`BOUNDED` read on a follower returns the uncommitted value, and after the leader rolls back (rollback changes pages without logging them) the follower keeps showing it as committed until a later image of that page arrives. A node promoted in that window makes it the cluster's committed state. Reproduced over a 3-node in-memory cluster (log #301) | Replicate undo information: the WAL `Undo` record carries the replaced version (a WAL/replication format change with version gates, like page deltas), followers register in-progress and aborted transactions from `Begin`/`Abort` records and build undo from the stream, and a new leader undoes its predecessor's unfinished transactions. A `tests/ha` case that rolls back on the leader, reads each follower, and fails over |
