# Required in production. At least 8 characters.
# In development the default is nextsql-admin if this is unset.
NEXTSQL_ADMIN_PASSWORD=password

# Optional. HMAC secret for the admin session cookie.
# Defaults to NEXTSQL_ADMIN_PASSWORD, then a dev secret.
NEXTSQL_ADMIN_SECRET=secret
