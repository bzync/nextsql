-- Recorded only for a dirty (SIGKILLed) fixture, on top of queries.sql.
SELECT id, note FROM replay ORDER BY id
SELECT id, name FROM customers ORDER BY id
SELECT id, total FROM orders ORDER BY id
SELECT region, id FROM events ORDER BY region, id
